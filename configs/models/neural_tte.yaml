type: neural_multihead
encoder: {type: mlp, latent_dim: 64, hidden_dims: [128, 64], dropout: 0.10}
survival_heads: [cox, aft, fht]
distillation: {enabled: true, start_epoch: 40, ramp_epochs: 20, weight: 0.05}
